#include "common.h"
#include <stdio.h>

// Basic stdio functions
int printf(const char *format, ...) {
    // Placeholder
    return 0;
}

int fgets(char *s, int size, FILE *stream) {
    // Placeholder
    return 0;
}